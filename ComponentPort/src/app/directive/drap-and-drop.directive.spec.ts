import { DrapAndDropDirective } from './drap-and-drop.directive';

describe('DrapAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new DrapAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
